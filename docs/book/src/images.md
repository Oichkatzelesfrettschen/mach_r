# Images & Artifacts

See `synthesis/docs/IMAGES.md` for detailed artifact descriptions.

Common commands:
- `cargo run -p xtask -- filesystem`
- `cargo run -p xtask -- disk-image`
- `cargo run -p xtask -- iso-image`
